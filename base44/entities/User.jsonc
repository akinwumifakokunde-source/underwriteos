{
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "organization_id": {
      "type": "string",
      "description": "Organization this user belongs to (set during onboarding)."
    }
  },
  "required": [
    "role"
  ],
  "name": "User",
  "type": "object"
}